interface:
  display_name: "PaperJury"
  short_description: "Pre-submission paper review skill"
  default_prompt: "Use $paperjury to review my CS paper before submission."
policy:
  allow_implicit_invocation: true
